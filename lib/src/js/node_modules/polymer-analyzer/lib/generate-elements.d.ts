import * as jsonschema from 'jsonschema';
import { Elements } from './elements-format';
import { Element as ResolvedElement } from './model/model';
export declare function generateElementMetadata(elements: ResolvedElement[], packagePath: string): Elements | undefined;
export declare class ValidationError extends Error {
    errors: jsonschema.ValidationError[];
    constructor(result: jsonschema.ValidatorResult);
}
/**
 * Throws if the given object isn't a valid AnalyzedPackage according to
 * the JSON schema.
 */
export declare function validateElements(analyzedPackage: Elements | null | undefined): void;
